eureka:
    client:
        serviceUrl:
            defaultZone: http://localhost:8760/eureka/
server:
    port: 8761

spring:
    application:
        name: service-helloworld
    cloud:
        config:
          label: master
          profile: dev
          discovery:
            enabled: true
            service-id: config-server