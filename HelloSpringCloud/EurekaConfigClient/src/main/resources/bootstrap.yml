eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      url: http://localhost:8888/
      enabled: true

server:
  port: 8882