eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/
server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/huangbin082/Bin
          searchPaths: HelloSpringCloud/EurekaConfig
application:
    name: config-server